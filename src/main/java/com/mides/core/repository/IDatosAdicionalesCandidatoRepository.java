package com.mides.core.repository;

import com.mides.core.model.DatosAdicionalesCandidato;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IDatosAdicionalesCandidatoRepository extends JpaRepository<DatosAdicionalesCandidato, Long> {
}
